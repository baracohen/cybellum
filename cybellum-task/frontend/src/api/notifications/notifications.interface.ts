export type UserType = {
  email: string;
  id: number;
};

export type NotificationResponseType = {
  id: string,
  title: string,
  description: string,
  created: string,
};

export type NotificationRequestType = {
  id: string;
};
